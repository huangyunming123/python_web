js 中写代码 是没有 浏览器里写js 代码的环境的   所以需要补环境  bom  （navigator 、document.history frames那些）


F12 打开
       应用面板是用来查看 删除cookies

       源代码页面

断点：
    dom断点   dom 事件断点   元素-> 右边时间监听器 里 click 找button

    xhr断点   包含某段代码块  的请求事件前  源代码/来源 ->断点 来包含


时间轴:
    加载html  ->  载js  ->  运行js初始化  ->  用户触发某个事件(dom断点)  ->  调用某段js  ->  加密函数  ->  给服务器发请求(XHR-SEND)(XHR断点)  ->  接收到服务器数据  ->  解密函数  ->  网页渲染

根据时间轴中的断点位置  才知道是 往前还是往后找加密


无限debug 定时器  function ff(){debugger} setInterval(ff,100);  鼠标右击 一律不在此处暂停  /  添加条件断点  / 重写定时器方法（断点需要打在方法执行前面)

npm install express -s (-s 局部变量 能够快速访问到 会安装到 node安装目录下)


括号调用  权重  aa() //逗号表达式

例如  (0，aa)() 等效于 aa()

遇到了  i[jt](i[qt](a,b)) 这种  其实 就是 i[jt] 是一个function   括号里是传参


遇到异步有2种可能  1种是异步代码里执行    一种是拦截器  如果是 n.then() 那么就是在拦截器里  有6个方法  取第一个